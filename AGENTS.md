# AGENTS.md

이 저장소는 일반적인 코드 프로젝트가 아니라 **LLM이 읽고 쓰는 지식 위키**입니다 (Karpathy의 "LLM Wiki" 패턴). Laravel + Domain-Driven Design 지식을 원본 소스(책, PDF, 아티클)로부터 정제해 `wiki/` 아래 마크다운 페이지로 영구 축적합니다.

Claude Code에서는 `.claude/commands/{ingest,lint,query}.md`가 `/ingest`, `/lint`, `/query` 슬래시 커맨드로 등록되어 있습니다. 이 파일은 그 세 워크플로를 Codex 등 슬래시 커맨드를 지원하지 않는 에이전트에서도 자연어 요청만으로 동일하게 수행할 수 있도록 요약한 것입니다. 사용자가 "위키에 이 파일 추가해줘", "위키에서 질문에 답해줘", "위키 점검해줘" 같은 요청을 하면 아래 해당 절차를 따르세요.

## 디렉토리 구조

```
laravel/
├── schema.md          ← 위키 구조/페이지 형식 정의 (필독)
├── index.md           ← 카테고리별 페이지 카탈로그 + 통계
├── log.md             ← 변경 이력 (append-only, 최신이 위)
├── sources/           ← 원본 소스 (불변)
└── wiki/
    ├── ddd-core/      ← DDD 핵심 개념
    ├── laravel/       ← Laravel 통합 및 구현 패턴
    ├── architecture/  ← 아키텍처 패턴
    ├── patterns/      ← 구현 패턴
    ├── testing/       ← 테스트 전략
    ├── tooling/       ← 정적 분석, CI/CD 등 개발 도구
    └── frontend/      ← Laravel 프로젝트의 프론트엔드 스택 (Tailwind, NativePHP 레이아웃 등)
```

각 위키 페이지의 frontmatter/본문 형식은 `schema.md`에 정의되어 있으니, 페이지를 새로 만들거나 갱신하기 전에 반드시 확인하세요. 요지:

```markdown
---
title: 페이지 제목
category: ddd-core | laravel | architecture | patterns | testing | tooling | frontend
tags: [tag1, tag2]
related: [[관련페이지1]], [[관련페이지2]]
---

# 제목
한 줄 요약.
## 핵심 개념
## Laravel 구현
## 주의사항 / 안티패턴
## 참고
```

## 워크플로 1 — 소스 흡수 (`/ingest` 대응)

사용자가 새 책/PDF/아티클을 위키에 추가해달라고 하면:

1. 파일을 `sources/`에 `YYYY-MM-DD_제목.확장자` 형식으로 복사한다 (원본은 불변).
2. 내용을 읽고 분석한다: 어떤 DDD/Laravel 개념을 다루는가? `index.md`를 참고해 기존 페이지 중 갱신할 것과 새로 만들 것을 정한다.
3. 위키 페이지를 생성/갱신한다 (보통 10–15개). 기존 페이지는 내용을 삭제하지 말고 보완한다. 새 페이지는 위 형식을 따르고 적절한 `wiki/` 하위 카테고리에 둔다. 페이지 간 `[[링크]]`를 연결한다.
4. `index.md`의 해당 카테고리 표에 새 페이지를 추가하고, 통계(전체 페이지 수, 최종 업데이트 날짜)를 실제 파일 수로 갱신한다.
5. `log.md` 맨 위에 `[YYYY-MM-DD] [INGEST] 소스명 — 생성/갱신된 페이지: A, B, ...` 형식으로 기록한다.
6. 처리한 소스, 새 페이지, 갱신 페이지, 추가로 필요한 페이지 제안을 요약해 보고한다.

## 워크플로 2 — 질의응답 (`/query` 대응)

사용자가 위키 지식을 바탕으로 질문하면:

1. 질문의 핵심 키워드와 관련 개념을 파악한다.
2. `index.md`로 관련 카테고리/페이지를 찾고, 해당 `wiki/` 페이지를 읽는다. 페이지 내 `[[링크]]`를 따라 연결된 페이지도 확인한다. 필요하면 `log.md`에서 최근 변경 이력도 참고한다.
3. 핵심 개념 설명 + Laravel 구현(코드 예제) + 주의사항/안티패턴 + 관련 페이지 링크(`[[페이지명]]`) 형식으로 답변을 종합한다.
4. 답변 과정에서 위키에 없는 유의미한 개념을 발견하면 새 페이지 생성을, 기존 페이지에 보완할 내용을 발견하면 갱신을, 끊어진 링크를 발견하면 수정을 제안한다. 사용자가 동의하면 반영하고 `log.md`에 `[YYYY-MM-DD] [ADD]` 또는 `[UPDATE]`로 기록한다.

## 워크플로 3 — 위키 정리 (`/lint` 대응)

사용자가 위키 점검/정리를 요청하면 (`--fix` 또는 "고쳐줘"가 붙으면 자동 수정까지 진행):

1. `wiki/`의 모든 `.md` 파일을 수집한다.
2. 다음을 점검한다:
   - **끊어진 링크**: `[[링크]]`가 가리키는 파일이 실제로 존재하는지
   - **고아 페이지**: `index.md`에 없거나 다른 어떤 페이지에서도 링크되지 않는 페이지
   - **프론트매터 누락**: `title`/`category`/`tags`/`related` 필드
   - **모순된 내용**: 같은 주제를 다루는 페이지 간 설명 불일치
   - **index.md 누락/불일치 항목**: 실제 파일과 `index.md` 카탈로그 간 차이
   - **오래된 통계**: `index.md`의 페이지 수/날짜가 실제와 다른지
3. 문제를 오류/경고/정보로 분류해 보고한다.
4. 자동 수정을 요청받았다면 안전하게 고칠 수 있는 것(끊어진 링크 언링크, frontmatter 보완, index 등록/통계 갱신, 고아 페이지 역링크 추가)을 적용하고 `log.md`에 `[YYYY-MM-DD] [LINT] N개 항목 수정 — ...` 형식으로 기록한다. 모순은 어느 쪽이 맞는지 먼저 사용자에게 확인한다.

## 원칙

- `sources/`의 원본 파일은 절대 수정하지 않는다 (불변).
- 위키 페이지를 갱신할 때 기존 내용을 함부로 삭제하지 않는다 — 보완한다.
- 이 위키가 반복적으로 강조하는 두 원칙(`wiki/ddd-core/Design Philosophy.md` 참고)을 새 페이지 작성 시에도 따른다: ① 패턴보다 도메인 언어가 먼저다, ② 이론적 순수성보다 프로젝트 전체의 일관성이 우선이다.
