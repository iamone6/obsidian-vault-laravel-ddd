---
title: 환영합니다!
type: meta
---

# 환영합니다!

이 보관함은 Obsidian 노트가 아니라 **LLM이 읽고 쓰는 지식 위키**입니다. Karpathy가 제안한 "LLM Wiki" 패턴을 기반으로, Laravel + DDD(Domain-Driven Design) 지식을 원본 소스(책, PDF)로부터 정제해 재사용 가능한 형태로 영구 축적합니다.

## 1. 이 위키에는 무엇이 있는가

**주제**: Laravel 프레임워크로 Domain-Driven Design을 실천하는 방법 — DDD 핵심 개념부터 Laravel 특유의 구현 패턴, 아키텍처, 테스트 전략, 개발 도구까지.

**등록된 원본 소스** (`sources/README.md` 참고, 8개):
- 처음부터 제대로 배우는 라라벨 (Matt Stauffer) — 라라벨 전반 종합 레퍼런스
- Domain-Driven Design with Laravel (Martin Joo) — DDD 실전서
- Case Study - Portfolio And Dividend Tracker (Martin Joo)
- Testing Complex Features (Martin Joo)
- Static Analysis And CI/CD Pipelines (Martin Joo)
- Layered Architectures with Laravel (Martin Joo)
- Laravel Eloquent Recipes (Martin Joo)
- Proper API Design With Laravel (Martin Joo)

**정제된 위키 페이지**: 현재 37개, 6개 카테고리로 분류됩니다 ([[index.md]]에서 전체 카탈로그 확인).

| 카테고리 | 내용 |
|---|---|
| `wiki/ddd-core/` | Entity, Value Object, Aggregate, Repository, Bounded Context 등 DDD 핵심 개념 + [[Design Philosophy]](이 위키를 관통하는 두 원칙: 언어 우선, 패턴은 가이드라인) |
| `wiki/laravel/` | Action Pattern, DTO, Custom Query Builder, ViewModel, Service Provider, Container, Eloquent Recipes, API Design 등 Laravel 구현 패턴 |
| `wiki/architecture/` | Layered Architecture, CQRS |
| `wiki/patterns/` | Repository Implementation, Mapper Pattern, Unit of Work, Pipeline Pattern, State Pattern |
| `wiki/testing/` | Domain/Feature Testing, Testing Complex Features, TDD |
| `wiki/tooling/` | 정적 분석(PHPInsights, Larastan, Deptrac), CI/CD 파이프라인 |

각 페이지는 핵심 개념 → Laravel 구현(코드 예제) → 주의사항/안티패턴 → 관련 페이지(`[[링크]]`) 순서로 구성되며, `related` 프론트매터와 본문 링크로 서로 연결되어 있습니다. 구조 전체 규칙은 [[schema.md]]에 정의되어 있습니다.

## 2. Claude Code와 Codex에서 사용하려면

### Claude Code

별도 설정이 필요 없습니다. 이 폴더를 작업 디렉토리로 열면 `.claude/commands/`에 있는 세 슬래시 커맨드가 자동으로 인식됩니다.

```
.claude/commands/
├── ingest.md   ← /ingest
├── lint.md     ← /lint
└── query.md    ← /query
```

바로 `/query <질문>`, `/ingest <파일경로>`, `/lint`를 치면 됩니다.

### Codex

Codex는 Claude Code의 `.claude/commands/` 슬래시 커맨드 메커니즘을 지원하지 않습니다. 대신 저장소 루트의 `AGENTS.md`를 표준 관례로 읽어 지속적 컨텍스트로 삼습니다. 그래서 이 위키를 Codex에서도 동일하게 쓰려면:

1. 저장소 루트에 `AGENTS.md`를 두고, 위키 스키마와 세 가지 워크플로(ingest/query/lint)를 요약해 담아둔다 — **이미 만들어 두었습니다** (`AGENTS.md` 참고).
2. Codex 세션을 이 폴더에서 시작하면 `AGENTS.md`가 자동으로 컨텍스트에 포함되므로, `/ingest` 같은 슬래시 문법 대신 자연어로 "새 소스 파일을 위키에 흡수해줘" 또는 "위키에서 XXX에 대해 답을 찾아줘"라고 요청하면 됩니다.
3. `.claude/commands/*.md`의 절차를 더 세밀하게 따르길 원한다면, 프롬프트에서 직접 "`.claude/commands/ingest.md`의 절차를 그대로 따라줘"처럼 특정 파일을 지목해도 됩니다.

## 3. 지식을 통합하고 사용하는 법

이 위키는 세 가지 반복 워크플로로 굴러갑니다.

### `/ingest` — 새 지식 추가

책, 아티클, PDF 등 새 소스를 `sources/`에 등록하고 위키 페이지 10–15개를 생성/갱신합니다.

```
/ingest sources/새로운파일.pdf
```

인자 없이 실행하면 아직 처리되지 않은 `sources/` 파일을 자동으로 찾아 처리합니다.

### `/query` — 위키에 질문하기

이미 축적된 지식으로 질문에 답합니다. 관련 페이지를 찾아 읽고, `[[링크]]`를 따라가며 종합적인 답을 만듭니다.

```
/query Repository와 Custom Query Builder 중 뭘 써야 해?
```

답변 중 위키에 없는 개념을 발견하면 새 페이지 생성을 제안하고, 동의하면 그 자리에서 위키를 보완합니다 (실제로 [[Design Philosophy]], [[Aggregate]]의 설계 체크리스트 섹션이 이렇게 만들어졌습니다).

### `/lint` — 위키 정리

끊어진 링크, 고아 페이지, 프론트매터 누락, 모순된 설명, `index.md` 통계 오류를 점검합니다.

```
/lint          ← 점검만
/lint --fix    ← 자동 수정 가능한 항목 즉시 수정
```

새 소스를 여러 개 연달아 흡수한 뒤에는 한 번씩 돌려보는 것을 권장합니다.

---

**요약**: 궁금한 게 있으면 `/query`, 새로 배운 걸 넣고 싶으면 `/ingest`, 위키가 지저분해진 것 같으면 `/lint`. Codex에서는 같은 역할을 자연어 요청 + `AGENTS.md`로 수행합니다.
